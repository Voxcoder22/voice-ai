import Home from './pages/Home';
import './styles/global.css';

/**
 * App — Root component
 * Currently renders only the Home page.
 * Extend here when adding routing (react-router-dom) for multi-page support.
 */
export default function App() {
  return <Home />;
}
